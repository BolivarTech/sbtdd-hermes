"""
Handlers for slash commands:
  /sbtdd
  /sbtdd-init
  /sbtdd-check
"""

# Placeholder - will be filled in Task 4

def _make_sbtdd_handler(ctx):
    def handler(args):
        return "SBTDD handler placeholder"
    return handler

def _make_sbtdd_init_handler(ctx):
    def handler(args):
        return "SBTDD-INIT handler placeholder"
    return handler

def _make_sbtdd_check_handler(ctx):
    def handler(args):
        return "SBTDD-CHECK handler placeholder"
    return handler
